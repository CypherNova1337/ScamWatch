## What this changes

## Why

## If this changes detection

- [ ] `python3 -m unittest` passes
- [ ] Added a regression test naming the real page that motivated the change
- [ ] Checked the change against the legitimate pages already in the suite
      (repair shops, vendor advisories, Vanguard, the blog post) and none of
      them started passing the gate
